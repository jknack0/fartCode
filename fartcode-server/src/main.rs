//! fartcode-server — remote workspace daemon (Phase 3, E12-08).
//!
//! Phase 0 placeholder binary. Exists so the workspace compiles end-to-end.

fn main() {
    println!("fartcode-server: placeholder (Phase 3 — workspace daemon)");
}
