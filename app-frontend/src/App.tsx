// Emdash app shell — Phase 0 bootstrap placeholder.
// Sidebar, task tree, and project view arrive with their tickets (E1-04, E2-08).
function App() {
  return (
    <main className="shell">
      <h1>Emdash</h1>
      <p className="muted">
        Workspace bootstrap (E0) — frontend skeleton running. Features land with
        Phase 0 tickets.
      </p>
    </main>
  );
}

export default App;
